/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VideoLengthToSecondsPipe } from './video-length-to-seconds.pipe';

describe('Pipe: VideoLengthToSeconds', () => {
  it('create an instance', () => {
    let pipe = new VideoLengthToSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
